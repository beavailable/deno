name: Update

on:
  workflow_dispatch:
    inputs:
      force_release:
        required: false
        type: boolean
        default: false
  schedule:
    - cron: '33 * * * *'

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Update
        id: update
        run: ./update.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ENV_FORCE_RELEASE: ${{ inputs.force_release }}
      - name: Upload to OBS
        if: ${{ steps.update.outputs.release == 'true' }}
        uses: beavailable/obs-upload@main
        with:
          pre-run: git add deno_* completions && git -c user.name='beavailable' -c user.email='beavailable@proton.me' commit -m 'Add files'
          username: beavailable
          password: ${{ secrets.OBS_PASSWORD }}
          project: home:beavailable
          package: deno
          type: deb
